{
    "name": "browser-script",
    "version": "0.1.0",
    "type": "module",
    "main": "dist/main.js",
    "scripts": {
        "build": "tsc && vite build",
        "lint": "eslint --ext ts,tsx src",
        "test": "vitest run"
    },
    "devDependencies": {
        "browser-lib": "workspace:^0.1.0"
    }
}
